current_branch=$(git rev-parse --abbrev-ref HEAD)

# ... (el resto del script igual)
if [ "$current_branch" = "main" ]; then
  echo "🔍 Revisando calidad en MAIN..."
  # Quitamos el bloqueo total, el build se encargará de la limpieza real
  npm run lint
else
  exit 0
fi